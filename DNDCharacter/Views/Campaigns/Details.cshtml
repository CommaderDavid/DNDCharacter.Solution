@{
    Layout = "_Layout";
}

@model DNDCharacter.Models.Campaign

<h1>Campaign Details</h1>
<hr />
<h2>Campaign Name: @Html.DisplayFor(model => model.Name)</h2>
<h3>Campaign Setting: @Html.DisplayFor(model => model.Setting)</h3>

@if (@Model.Characters.Count == 0)
{
    <p>There are no characters added to this campaign.</p>
}
else
{
    <h4>Characters present in the Campaign:</h4>
    @foreach (var join in Model.Characters)
    {
        @* @using (Html.BeginForm())
        {
            @Html.HiddenFor(model => model.ItemId)
            @Html.HiddenFor(model => model.Description)
            @Html.Label("Is the Item complete?")
            @Html.CheckBoxFor(model => model.ItemComplete)
            <br>
            <input type="submit" value="Save" />
        } *@
        <ul>
            <li>@join.Character.CharacterName</li>
        </ul>
    }
}

<p>@Html.ActionLink("Back to campaigns", "Index")</p>
<p>@Html.ActionLink("Edit campaign", "Edit", new { id = Model.CampaignId })</p>
<p>@Html.ActionLink("Delete Campaign", "Delete", new { id = Model.CampaignId })</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p>